from app.data_models.schemas import QueryRequest


def build_semantic_context(request: QueryRequest) -> str:
    # Placeholder: map user question to metrics, dimensions, and table mappings.
    return "Use business metrics and dimensions from the semantic layer."

